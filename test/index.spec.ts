describe('index - fetch', () => {
	describe('when requesting text source', () => {
		it('should...', () => {

		});
	});
	describe('when requesting json source', () => {
		it('should...', () => {

		});
	});
	describe('when response does not include json og text header(s)', () => {
		it('should...', () => {

		});
	});
});